pipeline
{
    agent any
    stages{
        stage('clone'){
            steps{
                git 'https://github.com/minhvl1/test_jenkins.git'
                }
            steps{
            FROM centos:latest
            RUN apt-get -y update && apt-get -y install firefox && apt-get -y install vim}
        }
    }
}
